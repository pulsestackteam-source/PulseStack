name = "pulsestack"
main = "src/index.ts" # Укажи правильный путь к твоему файлу с кодом
compatibility_date = "2026-09-15"

# Важно: отключаем автоматическое определение фреймворка
[build]
command = "" 
watch_dir = "src"
